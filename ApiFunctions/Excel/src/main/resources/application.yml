server:
    port: 8080
    shutdown: graceful

spring:
    data:
        mongodb:
            host: localhost
            port: 27017
            database: ExcelFunc

    servelet:
        multipart:
            max-file-size: 16MB
            max-request-size: 16MB
            enabled: true
